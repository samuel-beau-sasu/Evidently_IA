[project]
name = "exam-evidently"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "evidently==0.6.7",
    "pandas==2.1.4",
]

[dependency-groups]
dev = [
    "black>=24.10.0",
    "flake8>=7.0.0",
]
